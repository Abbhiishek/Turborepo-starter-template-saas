# Database credentials owned by @workspace/db.
DATABASE_URL="postgres://user:password@localhost:5432/app"

# Optional separate database for Mastra storage. This is still a database
# credential, so it stays with @workspace/db instead of @workspace/ai.
AI_DATABASE_URL=""
